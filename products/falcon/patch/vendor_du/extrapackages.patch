From e5cf1f464ffe87bc70bfdfb6b87945ebf96fad97 Mon Sep 17 00:00:00 2001
From: ThrillerAtPlay <thrilleratplay@gmail.com>
Date: Sat, 30 Apr 2016 22:20:07 +0000
Subject: [PATCH] Add a hook to add extra packages

This commit provides a way of customizing Dirty Deeds (unofficial) builds
without the need to maintain a fork of vendor_du.

As an example, https://github.com/Thrilleratplay/vendor_extra, I am
changing a few default settings, adding SlimLauncher and replacing
SuperSu with Superuser.

Change-Id: Id5034ccdf401a76b5fbd519e1716e28f1fcdb5e8
---
 config/common.mk | 3 +++
 1 file changed, 3 insertions(+)

diff --git a/config/common.mk b/config/common.mk
index affe122..85255c0 100644
--- a/config/common.mk
+++ b/config/common.mk
@@ -93,6 +93,9 @@ PRODUCT_PACKAGES += \
     DU-Updater
 endif
 
+# easy way to extend to add more packages
+-include vendor/extra/product.mk
+
 # Set all versions
 DU_VERSION := DU_$(DU_BUILD)_$(ANDROID_VERSION)_$(shell date -u +%Y%m%d-%H%M).$(DU_VERSION)-$(DU_BUILD_TYPE)
 DU_MOD_VERSION := DU_$(DU_BUILD)_$(ANDROID_VERSION)_$(shell date -u +%Y%m%d-%H%M).$(DU_VERSION)-$(DU_BUILD_TYPE)
